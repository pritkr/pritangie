# Implementation Plans

Generated by the improve skill on 2026-08-03 (re-baselined to the working tree
on 2026-08-04). Plans 001-005 cover content/direction improvements (the workshop
section is explicitly excluded). Plans 006-009 cover gaps the first pass skipped:
distribution (RSS + sitemap), PWA correctness, a typecheck gate, and dropping
an unused-heavy dependency. Execute in the order below unless dependencies say
otherwise. Each executor: read the plan fully before starting, honor its STOP
conditions, run every verification command, and update your row when done
(unless a reviewer is maintaining the index for you).

## Working-tree note for executors

Executors work IN-PLACE in the live working tree, NOT a git worktree. Treat the
current on-disk file contents as the baseline, NOT git HEAD (HEAD is stale).
Do NOT commit, stash, branch, or run destructive git commands. The user has
many uncommitted changes that must be preserved.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Clarify career positioning and primary CTA | P1 | M | — | DONE (hero sub-line, résumé CTA, CTA copy, About opening; stars standardized to 270) |
| 002 | Turn featured projects into evidence-rich case studies | P1 | M/L | 001 | DONE (role/outcome on all 4 cards + /projects/predirect case study) |
| 003 | Fix article previews and per-post metadata | P1 | S/M | — | DONE (3 post descriptions + per-post Layout metadata) |
| 004 | Replace weak social proof and sharpen personal voice | P1 | M | 001 | DONE (Step 2 — quote swap — skipped per owner decision; Service reordered) |
| 005 | Improve accessibility, external asset resilience, and quality gates | P2 | S/M | 001-004 | TODO (Step 4 superseded by 008) |
| 006 | Add an RSS feed (sitemap half already done in working tree) | P2 | S | (008) | TODO |
| 007 | Fix the PWA manifest theme color and add an offline fallback | P2 | S | — | TODO |
| 008 | Add a real typecheck gate with `astro check` | P1 | S/M | — | DONE (0 errors; also fixed broken Service.astro recs links) |
| 009 | Replace the GSAP marquee with CSS and drop the GSAP dependency | P2 | S | — | DONE (already in working tree) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency notes

- Plan 002 uses the role and positioning chosen in Plan 001.
- Plan 004 uses the same primary audience and professional identity as Plan 001.
- Plan 005 is intentionally last (among 001-005) so final checks cover the
  preceding content changes. Its Step 4 (quality gate) is superseded by Plan 008.
- Plan 008 is foundational: land it first so Plans 006/007 can include
  `bun run check` in their done criteria. 008 supersedes Plan 005 Step 4.
- Plans 006, 007 are independent of each other and of 001-005; they can run in
  parallel after 008.
- Plan 009 is already satisfied in the working tree (Tools.astro rewritten to a
  pure-CSS marquee; gsap removed from src and package.json) — no executor needed.

## Route-count note for executors

The build now emits **9** pages (`/`, `/about/`, `/blog/`, `/posts/post-1..3/`,
`/projects/`, `/projects/predirect/`, `/404.html`) plus `rss.xml` and
`sitemap-index.xml`. Earlier plans' "7/8 static routes generated" expected
output is stale — expect 9.

## Explicitly excluded

The workshop section is excluded from this batch, including
`src/components/WorkshopBooking.astro` and the workshop-related copy, links, and
booking flow. Do not modify it in any plan.

## Findings considered and rejected

- Full workshop booking redesign: explicitly excluded by the user for this batch.
- Replacing the entire visual design: the existing brutalist visual language is a
  strength; improve clarity and trust within it.
- Renaming `src/sample.ts` to a `src/data/site.ts` config (finding N6): low
  leverage and touches 5 importers; deferred unless the user opts in.
- Audience-segmented site restructure (direction D1): strategic, large effort,
  and tensions with Plan 001's single-identity approach — left as a design
  decision for the maintainer, not auto-planned.
- A `/now` page (direction D2): grounded and cheap, but offered as an option
  rather than planned; say the word to turn it into a plan.

## Reconciliation log (2026-08-04)

- Vetted all 9 plans against the live working tree (HEAD `05c4424` is stale).
- 009: already done in the working tree (Tools.astro CSS marquee, gsap gone) ->
  marked DONE.
- 006: sitemap half already done (@astrojs/sitemap wired, hand-written
  public/sitemap.xml deleted, robots.txt correct) -> re-baselined to RSS-only.
- 008: Tools.astro `any` sites already gone -> re-baselined to the 2 remaining
  `any` sites (RecentBlog.astro, blog.astro); dropped Tools.astro from scope.
- 003, 007: no drift — valid as written.
